{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ $CLAUDE_FILE_PATH == *.py ]]; then python -m py_compile $CLAUDE_FILE_PATH && ruff check $CLAUDE_FILE_PATH; fi"
          }
        ]
      }
    ]
  }
}
