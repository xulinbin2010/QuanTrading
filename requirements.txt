# Legacy compatibility manifest only. The source of truth is pyproject.toml + uv.lock.
# Use `uv sync` / `uv run ...`; do not install this file directly with pip.
ib_insync
pymysql
pyarrow
yfinance
numpy
pandas
requests
lxml
html5lib
fastapi
uvicorn[standard]
apscheduler
python-dotenv
anthropic
